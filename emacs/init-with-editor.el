(setq load-path (append
                 (list
                  (concat emacs-git "packages/with-editor-20260601.1526/"))
                 load-path))
(load "with-editor.el")

(provide 'init-with-editor)
