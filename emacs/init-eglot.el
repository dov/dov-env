;; Configure eglot
(use-package eglot
  :ensure t
  :config
  ;; Optional: Add any additional configuration for eglot here
;  (add-hook 'python-mode-hook 'eglot-ensure)
  ;; Dov - I prefer to do this manually at the moment
  ;; Add more hooks for other languages as needed
)
